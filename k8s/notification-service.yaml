apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: amadop
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP

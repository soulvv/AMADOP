apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: amadop
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP

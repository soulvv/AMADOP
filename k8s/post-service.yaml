apiVersion: v1
kind: Service
metadata:
  name: post-service
  namespace: amadop
  labels:
    app: post-service
spec:
  type: ClusterIP
  selector:
    app: post-service
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
